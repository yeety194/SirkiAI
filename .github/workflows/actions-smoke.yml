name: Actions smoke

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/actions-smoke.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: echo "actions-ok" && uname -a
